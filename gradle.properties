org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
# MC 26.1 requires Java 25; Gradle 9.6.1 runs on it, so use the Java 25 on PATH (no pin).

# 26.1 uses official Mojang mappings (Mojmap) — Yarn has no 26.x release.
minecraft_version=26.1
loader_version=0.19.3

mod_version=1.0.0
maven_group=nomad.client
archives_base_name=nomad

# Dependencies
fabric_version=0.145.1+26.1
netty_version=4.1.90.Final
